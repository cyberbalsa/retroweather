name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: JS lint and tests
        run: npm ci && npm test

      - name: Android unit tests
        run: ./gradlew test

      - name: Assemble release
        run: ./gradlew assembleRelease bundleRelease

      - name: Add Android build-tools to PATH
        run: |
          BUILD_TOOLS_DIR="$(ls -d "$ANDROID_SDK_ROOT/build-tools"/*/ 2>/dev/null | sort -V | tail -1)"
          if [ -z "$BUILD_TOOLS_DIR" ]; then
            echo "::error::Could not locate Android build-tools under ANDROID_SDK_ROOT=${ANDROID_SDK_ROOT}"
            exit 1
          fi
          echo "$BUILD_TOOLS_DIR" >> "$GITHUB_PATH"

      - name: Decode keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > "$RUNNER_TEMP/keystore.jks"

      - name: Sign APK
        env:
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          apksigner sign \
            --ks "$RUNNER_TEMP/keystore.jks" \
            --ks-key-alias "$KEY_ALIAS" \
            --ks-pass "env:STORE_PASSWORD" \
            --key-pass "env:KEY_PASSWORD" \
            --out app/build/outputs/apk/release/app-release-signed.apk \
            app/build/outputs/apk/release/app-release-unsigned.apk

      - name: Sign AAB
        env:
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          STORE_PASSWORD: ${{ secrets.STORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          printf '%s' "$STORE_PASSWORD" > "$RUNNER_TEMP/store.pwd"
          printf '%s' "$KEY_PASSWORD"   > "$RUNNER_TEMP/key.pwd"
          jarsigner \
            -keystore "$RUNNER_TEMP/keystore.jks" \
            -storepass:file "$RUNNER_TEMP/store.pwd" \
            -keypass:file   "$RUNNER_TEMP/key.pwd" \
            -signedjar app/build/outputs/bundle/release/app-release-signed.aab \
            app/build/outputs/bundle/release/app-release.aab \
            "$KEY_ALIAS"
          rm -f "$RUNNER_TEMP/store.pwd" "$RUNNER_TEMP/key.pwd"

      - name: Verify APK signature
        run: |
          apksigner verify --verbose \
            app/build/outputs/apk/release/app-release-signed.apk

      - name: Clean up keystore
        if: always()
        run: rm -f "$RUNNER_TEMP/keystore.jks" "$RUNNER_TEMP/store.pwd" "$RUNNER_TEMP/key.pwd"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${{ github.ref_name }}"
          gh release create "$TAG" \
            --title "$TAG" \
            --notes "Release $TAG" \
            app/build/outputs/apk/release/app-release-signed.apk \
            app/build/outputs/bundle/release/app-release-signed.aab
